const velocidade = 75
const warn = 85;

    // condicao ? true: false;

    const condicao= velocidade >=warn
    ? console.log("voce esta acima da velocidade permitida")
    : console.log("voce esta na media da velocidade permitida");

//     if(velocidade>= warn){
//     console.log("Voce esta acima da velocidade permitida!");
// }
